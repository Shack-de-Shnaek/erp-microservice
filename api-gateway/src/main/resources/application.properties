spring.application.name=api-gateway
spring.cloud.consul.discovery.register=true
spring.cloud.consul.host=${CONSUL_HOST:localhost}
spring.cloud.consul.port=${CONSUL_PORT:8500}
spring.config.import=optional:consul:
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration,org.springframework.boot.autoconfigure.orm.jpa.HibernateJpaAutoConfiguration,org.springframework.boot.autoconfigure.jdbc.DataSourceTransactionManagerAutoConfiguration

spring.cloud.gateway.discovery.locator.enabled=true
spring.cloud.gateway.discovery.locator.lower-case-service-id=true

spring.security.oauth2.resourceserver.jwt.issuer-uri=${KEYCLOAK_ISSUER_URL:http://localhost:8082/realms/finki-services}
spring.security.oauth2.resourceserver.jwt.jwk-set-uri=${NIMBUS_JWK_SET_URL:http://localhost:8082/realms/finki-services/protocol/openid-connect/keys}

management.endpoints.web.exposure.include=health,info
